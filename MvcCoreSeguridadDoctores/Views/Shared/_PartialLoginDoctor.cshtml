@if (!Context.User.Identity.IsAuthenticated)
{
    <a asp-action="Perfil" asp-controller="Doctores" class="nav-link">Log In</a>
}
else
{
    <a asp-action="LogOut" asp-controller="Identity" 
       class="nav-link">
        Log Out @Context.User.Identity.Name
    </a>
}